import React from "react";
import styles from "./Button.module.scss";
const ButtonNext = () => {
  return (
    <button className={styles.NextButton}>
      <a>Следующий пример</a>
    </button>
  );
};

export default ButtonNext;
